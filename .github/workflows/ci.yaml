on: [push]
jobs:
  build_ros_package:
    runs-on: ubuntu-latest
    container:
      image: pedros235/unitree_ros:latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Build
        run: . /opt/ros/foxy/setup.bash && colcon build
